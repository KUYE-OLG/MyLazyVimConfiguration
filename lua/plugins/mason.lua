return {
  -- "mason-org/mason.nvim",
  -- opts = function(_, opts)
  --   opts.ensure_installed = vim.list_extend(opts.ensure_installed or {}, {
  --     "stylua", -- Add the packages you need
  --   })
  --   opts.ui = {
  --     icons = {
  --       package_installed = "✓",
  --       package_pending = "➜",
  --       package_uninstalled = "✗",
  --     },
  --   }
  --   opts.log_level = vim.log.levels.ERROR -- Retains log level settings
  -- end,
}
